# Методы списка в Python

## `.pop()` — удаление и возврат элемента  
Удаляет элемент по индексу (по умолчанию — последний) и **возвращает** его.

```python
my_list = [1, 2, 3, 4, 5]
last_element = my_list.pop()      # Удаляет и возвращает последний элемент
print(last_element)  # → 5
print(my_list)       # → [1, 2, 3, 4]

second_element = my_list.pop(1)   # Удаляет элемент с индексом 1
print(second_element)  # → 2
print(my_list)         # → [1, 3, 4]
```

---

## `.append()` — добавление в конец списка  
Добавляет один элемент в конец.

```python
my_list = [1, 2, 3]
my_list.append(4)
print(my_list)  # → [1, 2, 3, 4]
```

---

## `.insert()` — вставка по индексу  
Вставляет элемент **перед** указанным индексом.

```python
my_list = [1, 2, 4]
my_list.insert(2, 3)   # Вставить 3 на позицию 2 (между 2 и 4)
print(my_list)  # → [1, 2, 3, 4]
```

---

## `.remove()` — удаление по значению  
Удаляет **первое** вхождение указанного значения.

```python
my_list = [1, 2, 3, 2, 4]
my_list.remove(2)
print(my_list)  # → [1, 3, 2, 4]
```

> ⚠️ Если значение не найдено — ошибка `ValueError`.

---

## `.clear()` — полная очистка списка  
Оставляет список пустым.

```python
my_list = [1, 2, 3]
my_list.clear()
print(my_list)  # → []
```

---

## `.index()` — поиск индекса элемента  
Возвращает индекс **первого** вхождения значения.

```python
my_list = [10, 20, 30, 40]
print(my_list.index(30))  # → 2
```

> ⚠️ Если элемент отсутствует — `ValueError`.

---

## `.count()` — подсчёт вхождений  
Считает, сколько раз элемент встречается в списке.

```python
my_list = [1, 2, 2, 3, 2]
print(my_list.count(2))  # → 3
```

---

## `.sort()` — сортировка списка *на месте*  
Изменяет исходный список.

```python
my_list = [3, 1, 4, 2]
my_list.sort()
print(my_list)        # → [1, 2, 3, 4]

my_list.sort(reverse=True)
print(my_list)        # → [4, 3, 2, 1]
```

> 🔹 Работает только со списками, где все элементы сравнимы (например, только числа *или* только строки).

---

## `.reverse()` — реверс порядка элементов  
Изменяет список «задом наперёд».

```python
my_list = [1, 2, 3, 4]
my_list.reverse()
print(my_list)  # → [4, 3, 2, 1]
```

Альтернатива без изменения исходного списка: `reversed_list = my_list[::-1]`.

---

## `.copy()` — поверхностное копирование  
Создаёт **новый** список с теми же элементами.

```python
original = [1, 2, 3]
copy = original.copy()
copy.append(4)
print(original)  # → [1, 2, 3] — не изменился
print(copy)      # → [1, 2, 3, 4]
```

> 🔹 Не копирует вложенные объекты глубоко — для глубокого копирования нужен `copy.deepcopy()`.

---

## `.extend()` — добавление нескольких элементов  
Добавляет все элементы из другого итерируемого объекта (списка, строки, кортежа и т.д.).

```python
list1 = [1, 2, 3]
list2 = [4, 5, 6]
list1.extend(list2)
print(list1)  # → [1, 2, 3, 4, 5, 6]
```

Аналог: `list1 + list2` создаёт **новый** список, не изменяя `list1`.
