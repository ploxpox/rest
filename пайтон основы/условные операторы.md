## 📝 Условные операторы: `if`, `elif`, `else`

Условные операторы — это **развилки** в коде: в зависимости от условия программа идёт по одному из путей.

---

### 📘 Теория

#### 🔹 Основная структура:

```python
if условие_1:
    # выполняется, если условие_1 — True
elif условие_2:
    # выполняется, если условие_1 — False, а условие_2 — True
else:
    # выполняется, если все условия выше — False
```

- `elif` и `else` — **необязательны**.  
  Можно писать только `if`, или `if` + `else`, или полную цепочку.
- После `if`/`elif`/`else` **всегда** `:`  
- Код внутри блока — с **отступом** (4 пробела по стандарту).

#### 🔹 Что может быть *условием*?
Любое выражение, которое даёт `True` или `False`:

| Выражение            | Результат |
|----------------------|-----------|
| Сравнения: `>`, `<`, `==`, `!=`, `>=`, `<=` | `x > 5`, `name == "Tom"` |
| Логические операторы: `and`, `or`, `not` | `age >= 18 and has_id` |
| Проверка «истинности» значений: | `if name:` → `True`, если строка не пустая |

> 💡 В Python почти всё можно привести к `True`/`False`:  
> - **False**: `0`, `""`, `[]`, `{}`, `None`, `False`  
> - **True**: любое ненулевое число, непустая строка/список и т.д.

---

### 💡 Практика с пояснением

#### Пример: возраст и категории

```python
age = 17

if age >= 18:
    print("Вы совершеннолетний!")
elif age >= 13:
    print("Вы подросток.")
else:
    print("Вы ребёнок.")
```

🔹 Проверка идёт **сверху вниз** — как в реальной жизни: сначала главный критерий (18+), потом — следующий (13+), и только потом — всё остальное.  
🔹 Как только одно условие **выполнилось** — остальные блоки **пропускаются**.  
🔹 Результат при `age = 17`:  
 → `age >= 18`? **Нет**  
 → `age >= 13`? **Да** → вывод: `"Вы подросток."`

> ⚠️ **Важно:** порядок `elif` имеет значение!  
> Если поменять местами условия (`age >= 13` перед `age >= 18`), то 18-летние тоже попадут в «подростки».

---

### 🧩 Частые ловушки (и как их избежать)

#### ❌ Ошибка 1: одно `=` вместо `==`
```python
# ❌ ПЛОХО (это присваивание, не сравнение!)
if age = 18:  # SyntaxError!

# ✅ ПРАВИЛЬНО
if age == 18:
```

#### ❌ Ошибка 2: забытые `:` или отступы
```python
if age > 18  # ← пропущено :
    print("OK")  # ← будет ошибка

if age > 18:
print("OK")      # ← пропущен отступ → IndentationError
```

#### ❌ Ошибка 3: `if` вместо `elif` там, где нужно исключить варианты
```python
# ❌ Неправильно — оба условия могут сработать:
if score > 90:
    grade = "A"
if score > 80:     # ← даже если score = 95, сработает и это!
    grade = "B"

# ✅ Правильно — только один вариант:
if score > 90:
    grade = "A"
elif score > 80:
    grade = "B"
```

---

### ✅ Советы

- Используй `elif`, когда варианты **взаимоисключающие** («или-или»).  
- Используй отдельные `if`, когда условия **независимы** (могут быть истинны одновременно).  
- Для сложных условий — разбивай на части или вводи промежуточные переменные:

```python
is_adult = age >= 18
has_license = license_year > 2020

if is_adult and has_license:
    print("Можно водить!")
```
