### 📘 Теория: списковые включения (list comprehensions)

Списковое включение — это **лаконичный и выразительный способ** создать новый список на основе существующей последовательности (списка, строки, `range` и др.).

**Общий синтаксис:**

```python
[выражение for элемент in последовательность if условие]
```

- `выражение` — что сделать с каждым элементом (например, `x * 2`, `x.upper()`, `x**2`).  
- `for элемент in последовательность` — откуда брать данные.  
- `if условие` — *необязательный* фильтр: элемент попадает в новый список, **только если условие истинно**.

💡 Читать удобно слева направо — почти как на естественном языке:  
> *«Создай список из `[x**2]`, для каждого `x` в `range(1, 6)`, если `x` чётное».*

Это **не замена всем циклам**, а инструмент для простых, читаемых преобразований.

---

### 💡 Практика с пояснениями

#### ✔ Пример 1: квадраты чисел  
*Задача:* получить список квадратов чисел от 1 до 5.

```python
квадраты = [x**2 for x in range(1, 6)]
print(квадраты)  # → [1, 4, 9, 16, 25]
```

🔹 `range(1, 6)` → `1, 2, 3, 4, 5`  
🔹 `x**2` применяется к каждому значению  
🔹 Результат — **новый список**, исходный `range` не меняется.

---

#### ✔ Пример 2: квадраты **только чётных** чисел  
*Задача:* оставить квадраты только для чётных `x`.

```python
чётные_квадраты = [x**2 for x in range(1, 6) if x % 2 == 0]
print(чётные_квадраты)  # → [4, 16]
```

🔹 Условие `if x % 2 == 0` отсеивает нечётные (`1, 3, 5`).  
🔹 Остаются `x = 2` и `x = 4` → их квадраты: `4` и `16`.

> ⚠️ Важно: условие проверяется **до** вычисления выражения.  
> То есть сначала `x` проходит фильтр, *потом* для прошедших считается `x**2`.

---

#### ✔ Пример 3: буквы в верхнем регистре  
*Задача:* получить список заглавных букв из строки.

```python
строка = "Привет"
заглавные = [буква.upper() for буква in строка]
print(заглавные)  # → ['П', 'Р', 'И', 'В', 'Е', 'Т']
```

🔹 Строка — тоже последовательность! Перебираем посимвольно.  
🔹 `.upper()` — метод строки, возвращает заглавную версию.

---

### ✅ Преимущества списковых включений

- ✅ **Короче**, чем `for` + `append()` — меньше кода, меньше ошибок.  
- ✅ **Читаемее**, когда логика простая (преобразование + фильтр).  
- ✅ **Быстрее** — реализованы на уровне C, оптимизированы в CPython.  
- ✅ **Иммутабельны по духу** — создаётся *новый* список, оригинальные данные не трогаются.

> 🚫 Но не стоит «впихивать» сложную логику (вложенные условия, много действий) — это снижает ясность. В таких случаях лучше обычный цикл.
