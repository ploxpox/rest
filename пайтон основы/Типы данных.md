## 1. `list` — универсальный список  
**Когда:** почти всегда в начале — гибко, знакомо, удобно для изменений.

### ✅ Свойства  
- Изменяемый  
- Упорядоченный  
- Позволяет дубликаты и разные типы

### 💡 Пример
```python
shopping = ["milk", "bread"]
shopping.append("eggs")      # → ["milk", "bread", "eggs"]
shopping[0] = "soy milk"     # можно менять по индексу
```

> ⚠️ **Осторожно с копированием!**  
> `b = a` — это **ссылка**, не копия.  
> Правильно: `b = a.copy()` или `b = a[:]`.

---

## 2. `tuple` — неизменяемый «пакет»  
**Когда:** данные фиксированы — координаты, настройки, возврат из функции.

### ✅ Свойства  
- Неизменяемый  
- Упорядоченный  
- Быстрее и легче `list`

### 💡 Пример
```python
point = (10, 20)
host, port = ("localhost", 8080)  # распаковка!

def get_bounds():
    return 0, 100  # ← это кортеж (0, 100)
min_val, max_val = get_bounds()
```

> 🎯 Одна запятая — и кортеж: `(42,)` ≠ `42`

---

## 3. `set` — «мешок уникальных значений»  
**Когда:** нужны уникальные элементы или быстрая проверка «есть ли?».

### ✅ Свойства  
- Только уникальные элементы  
- Неупорядоченный  
- Очень быстрая проверка: `if x in my_set`

### 💡 Пример
```python
tags = {"python", "coding"}
tags.add("data")               # → {"python", "coding", "data"}
tags.add("python")             # дубликат игнорируется

# Общие друзья:
group1 = {"Alice", "Bob"}
group2 = {"Bob", "Charlie"}
common = group1 & group2       # → {"Bob"} (пересечение)
```

> 🔑 Поддерживает операции:  
> `|` — объединение, `&` — пересечение, `-` — разность, `^` — симметричная разность.

---

## 4. `dict` — «словарь: ключ → значение»  
**Когда:** структура данных с именованными полями (как JSON или объект).

### ✅ Свойства  
- Изменяемый  
- Доступ по **ключу**, не по индексу  
- Ключи — уникальны и хешируемы (`str`, `int`, `tuple`)

### 💡 Пример
```python
user = {
    "name": "Alice",
    "age": 30,
    "active": True
}

# Безопасное чтение:
email = user.get("email", "unknown@example.com")
# → "unknown@example.com", если ключа нет — не ошибка!

# Быстрый счётчик:
counts = {}
for word in "a b a c a".split():
    counts[word] = counts.get(word, 0) + 1
# → {'a': 3, 'b': 1, 'c': 1}
```

> 🎁 Python 3.7+: порядок вставки сохраняется.

---

## 5. `array.array` — компактный числовой список  
**Когда:** много чисел одного типа, важна память и скорость (но не нужен `numpy`).

### ✅ Свойства  
- Однотипный  
- Меньше памяти, чем `list`  
- Поддерживает почти все методы `list`

### 💡 Пример
```python
import array
temps = array.array('f', [20.1, 22.5, 19.8])  # 'f' — float
temps.append(21.0)
```

> 📌 Типы:  
> `'i'` — целые, `'f'` — float, `'d'` — double, `'b'` — байт.

---

## 6. `numpy.ndarray` — сила для вычислений  
**Когда:** математика, ML, обработка сигналов, изображений — всё, где нужны векторы/матрицы.

### ✅ Свойства  
- Векторизованные операции (без циклов!)  
- Поддержка многомерности  
- Огромная экосистема (`pandas`, `scipy`, `torch`)

### 💡 Пример
```python
data = [1, 2, 3, 4]

# Квадраты — через списковое включение:
squared = [x ** 2 for x in data]
# → [1, 4, 9, 16]

# Среднее арифметическое — через sum() и len():
mean = sum(data) / len(data)
# → 2.5

# Матрица как список списков:
matrix = [[1, 2],
          [3, 4]]

# Транспонирование — вручную (списковое включение + zip):
transposed = [list(row) for row in zip(*matrix)]
# → [[1, 3], [2, 4]]
```

> ⚙️ Требует установки: `pip install numpy`

---

# 🧠 Как выбрать — шпаргалка без таблиц

| Задача | Лучший выбор |
|--------|--------------|
| Хочу добавлять/удалять элементы — список покупок | `list` |
| Координаты, настройки, возврат из функции | `tuple` |
| Уникальные значения: теги, email-адреса | `set` |
| Описание объекта: пользователь, книга, товар | `dict` |
| Сотни тысяч чисел — температуры, датчики | `array.array` |
| Векторы, матрицы, ML, статистика | `numpy` |

> ✨ **Совет для новичка:**  
> Начинайте с `list` и `dict` — 90% задач они покрывают.  
> Остальные — когда появится **конкретная причина**: скорость, память, уникальность, неизменяемость.

---

# 🛠️ Полезные лайфхаки (без таблиц!)

### 🔁 Генераторы списков — короче и быстрее циклов
```python
# Вместо цикла:
squares = [x**2 for x in range(5)]        # [0, 1, 4, 9, 16]
evens = [x for x in range(10) if x % 2 == 0]
```

### 📖 Генераторы словарей
```python
names = ["Alice", "Bob"]
lengths = {name: len(name) for name in names}
# → {'Alice': 5, 'Bob': 3}
```

### ↔ Распаковка
```python
coords = (10, 20)
x, y = coords

# И даже:
a, *rest, b = [1, 2, 3, 4, 5]
# → a=1, rest=[2,3,4], b=5
```
