# 🧩 Функции в Python: от основ к гибкости

## 1. `def` — как объявить функцию

### 📘 Теория кратко
- Начинается с `def`, затем имя (`snake_case`), `()`, параметры, `:`
- Тело — с **отступом** (4 пробела)
- `return` — вернуть результат и **выйти**
- Если `return` нет (или просто `return`) → функция возвращает `None`

### 💡 Практика с пояснениями

```python
# 1. Простая функция с return
def multiply(a, b):
    return a * b  # ← как только выполнилось — функция закончилась

print(multiply(3, 4))  # → 12


# 2. Функция без параметров
def say_hello():
    return "Привет!"  # можно вернуть строку, число, список — что угодно

print(say_hello())  # → Привет!


# 3. Функция без return — возвращает None
def print_time():
    print("Текущее время: 10:30")
    # ← здесь нет return → неявно: return None

result = print_time()  # сначала напечатает время...
print(result)          # → None


# 4. Возврат нескольких значений — на самом деле это один кортеж
def get_min_max(numbers):
    if not numbers:
        return None, None  # ← (None, None) — кортеж из двух элементов
    return min(numbers), max(numbers)  # ← (минимум, максимум)

nums = [5, 2, 8, 1, 9]
minimum, maximum = get_min_max(nums)  # ← распаковка кортежа
print(f"Минимум: {minimum}, Максимум: {maximum}")  # → Минимум: 1, Максимум: 9
```

> ⚠️ `return` **немедленно** завершает функцию:
> ```python
> def example(x):
>     if x > 0:
>         return "положительное"
>     print("Это не выполнится, если x > 0")  # ← пропустится!
>     return "не положительное"
> ```

---

## 2. Параметры и аргументы: как передавать данные

### 📘 Что важно знать
- **Параметры** — имена в определении (`def func(a, b)` → `a`, `b` — параметры)  
- **Аргументы** — реальные значения при вызове (`func(1, 2)` → `1`, `2` — аргументы)  
- Бывают:  
  - **позиционные** — по порядку: `func(10, 20)`  
  - **именованные** — с указанием имени: `func(width=10, height=20)`  
  - **со значениями по умолчанию** — если не передали, используется «запасной» вариант

### 💡 Практика — запусти и посмотри

```python
def create_report(title, author="Аноним", priority="обычный"):
    return f"Отчет: '{title}' | Автор: {author} | Приоритет: {priority}"

# Только позиционные — строго по порядку
print(create_report("Анализ продаж", "Иван", "высокий"))
# → Отчет: 'Анализ продаж' | Автор: Иван | Приоритет: высокий

# Именованные — порядок не важен
print(create_report(priority="срочно", title="Ошибка сервера", author="Мария"))
# → Отчет: 'Ошибка сервера' | Автор: Мария | Приоритет: срочно

# Пропускаем необязательные — сработают значения по умолчанию
print(create_report("Ежедневный дайджест"))
# → Отчет: 'Ежедневный дайджест' | Автор: Аноним | Приоритет: обычный

# Смешанный вызов: сначала позиционные, потом именованные
print(create_report("Бюджет", priority="низкий"))
# → Отчет: 'Бюджет' | Автор: Аноним | Приоритет: низкий
```

> 🚫 **Ошибка:** нельзя передавать позиционный аргумент *после* именованного:  
> `create_report(author="Иван", "Анализ")` → **SyntaxError**

---

## 3. `*args` и `**kwargs` — «ловушки для неизвестного»

### 📘 Когда нужны?
Когда **не знаешь заранее**, сколько аргументов придёт — например, в универсальных функциях или декораторах.

- `*args` → собирает **все лишние позиционные** аргументы в **кортеж**  
- `**kwargs` → собирает **все лишние именованные** аргументы в **словарь**

> 📌 Имена `args`/`kwargs` — соглашение. Можно `*numbers`, `**options` — но обычно используют стандарт.

### 💡 Практика

```python
# 1. *args — сумма любого количества чисел
def sum_all(*numbers):  # numbers — это кортеж!
    print("Получили:", numbers)  # например: (1, 2, 3)
    total = 0
    for num in numbers:
        total += num
    return total

print(sum_all(1, 2, 3))      # → Получили: (1, 2, 3) → 6
print(sum_all())             # → Получили: () → 0


# 2. **kwargs — описание «на лету»
def describe_person(**info):  # info — это словарь!
    print("Данные:", info)  # например: {'name': 'Алексей', 'age': 25}
    desc = "Человек:\n"
    for key, value in info.items():
        desc += f"  {key}: {value}\n"
    return desc

print(describe_person(name="Алексей", age=25))
# → Человек:
#     name: Алексей
#     age: 25
```

> 🔁 Часто используют, чтобы **передать аргументы дальше**:
> ```python
> def wrapper(func, *args, **kwargs):
>     print("Перед вызовом...")
>     result = func(*args, **kwargs)  # ← распаковка!
>     print("После вызова")
>     return result
> ```

---

## 4. Область видимости: где живут переменные?

### 📘 Кратко и по делу
- **Глобальные** — вне функций → видны везде (но **изменять** внутри функции без `global` нельзя)  
- **Локальные** — внутри функции → видны **только внутри**  
- Если имя совпадает — внутри функции **локальная «затеняет» глобальную**  
- `global` — разрешает **изменять** глобальную переменную (но лучше **избегать**)

### 💡 Практика — смотри, где ошибки

```python
user_count = 0  # ← глобальная

# ❌ Плохо: попытка изменить глобальную без global
def add_user_bad(name):
    user_count = user_count + 1  # ← ОШИБКА! Python считает, что user_count — локальная,
                                 # но она ещё не создана → UnboundLocalError

# ✅ Хорошо: через параметры и return
def add_user_good(current, name):
    new_count = current + 1
    print(f"Добавлен: {name}")
    return new_count  # ← явно возвращаем новое значение

# Ещё вариант (редко!): с global
def add_user_global(name):
    global user_count  # ← «разрешение» менять глобальную
    user_count += 1
    print(f"(global) Добавлен: {name}")

# Работает:
count = 0
count = add_user_good(count, "Иван")  # → Добавлен: Иван
print(count)  # → 1
```

> ✅ **Рекомендация для начинающих:**  
> **Не используй `global`**. Передавай данные в функцию → обрабатывай → возвращай результат.  
> Это делает код предсказуемым и легко тестируемым.

---

## 5. Функции — это объекты 🎁

### 📘 Что это значит?
Функция в Python — не «команда», а **объект в памяти**, как число или строка. Её можно:

- Присвоить переменной  
- Передать как аргумент  
- Вернуть из другой функции  

Это основа для гибкого, переиспользуемого кода.

### 💡 Практика без импортов

```python
# 1. Присваиваем функцию переменной
def square(x): return x * x
op = square  # ← теперь op — это та же функция!
print(op(5))  # → 25


# 2. Передаём функцию как аргумент
def apply_op(numbers, func):
    return [func(x) for x in numbers]  # списковое включение

nums = [1, 2, 3]
print(apply_op(nums, square))   # → [1, 4, 9]
print(apply_op(nums, lambda x: x+10))  # → [11, 12, 13] (лямбда — короткая функция)


# 3. Возвращаем функцию из функции
def make_adder(n):
    def add(x):  # ← внутренняя функция (замыкание!)
        return x + n
    return add

add5 = make_adder(5)
print(add5(10))  # → 15
```

---

## 6. Аннотации типов — подсказки для людей

### 📘 Что это?
Подсказки, **какие типы** ожидает функция. Они:

- ❌ **не проверяются** при запуске (Python игнорирует их)  
- ✅ **помогают** тебе, коллегам и IDE понять код  
- ✅ снижают количество ошибок (особенно в больших проектах)

### 💡 Базовые примеры (встроенные типы)

```python
def greet(name: str) -> str:
    return f"Привет, {name}!"

def total(items: list) -> int:
    return sum(items)

def create_config(host: str, port: int, debug: bool = False) -> dict:
    return {"host": host, "port": port, "debug": debug}

def log_message(msg: str) -> None:  # ← ничего не возвращает
    print(f"[LOG] {msg}")
```

> 🔍 Всё это работает **без импортов** — `int`, `str`, `list`, `dict`, `None` — встроены.

> 📌 Совет: начинай с аннотаций для **параметров и возврата** — даже это сильно улучшает читаемость.

---

# ✅ Главное — запомни эти 5 правил

1. **Имя функции — глагол:** `calculate_total()`, `validate_input()`  
2. **Избегай `global`** — передавай → обрабатывай → возвращай  
3. **`return` завершает функцию** — всё после него не выполнится  
4. **`*args`, `**kwargs` — когда не знаешь, сколько аргументов придёт**  
5. **Аннотации — для людей, не для Python** — но они очень помогают  
