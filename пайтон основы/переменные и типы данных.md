# 🔤 Переменные

Переменные нужны для **хранения данных** в памяти.

### ✅ Правила именования:
- Должны начинаться с **буквы** (`a–z`, `A–Z`) или **подчёркивания** `_`;  
- Могут содержать буквы, цифры и `_`;  
- **Не могут** начинаться с цифры;  
- **Не могут** совпадать с **ключевыми словами** Python (`if`, `for`, `class`, `True`, `None` и др.).

```python
name = "Tom"          # строка
age = 30              # целое число
is_student = False    # логическое значение
```

### 📌 Стили написания имён:
- `snake_case` — рекомендуется в Python (PEP 8): `user_name`, `max_speed`  
- `camelCase` — допустим, но не по стандарту: `userName`, `maxSpeed`

> 💡 Имя переменной должно быть **понятным**: `price`, `total_count`, `is_ready` — лучше, чем `x`, `a1`, `tmp`.

---

# 🧩 Типы данных

Python — язык с **динамической типизацией**: тип определяется автоматически при присвоении.

### `bool` — логический тип  
Только два значения: `True` и `False` (с заглавной буквы!).

```python
is_active = True
has_errors = False
```

### `int` — целые числа  
Поддерживают разные системы счисления:

```python
a = 42               # десятичная (по умолчанию)
b = 0b101010         # двоичная → 42
c = 0o52             # восьмеричная → 42
d = 0x2A             # шестнадцатеричная → 42
```

### `float` — числа с плавающей точкой  
Дробные числа. Всегда используют **точку**, не запятую.

```python
height = 1.68
price = 99.99
```

> ⚠️ Из-за особенностей хранения `0.1 + 0.2 != 0.3` в точности — это не ошибка, а особенность IEEE 754.

### `str` — строки  
Последовательности символов в кавычках. Можно использовать `'` или `"`.

```python
message = "Hello"
name = 'Tom'
```

#### 🔹 Многострочные строки — через тройные кавычки:

```python
poem = """Laudate omnes gentes laudate
Magnificat in secula
Et anima mea laudate
Magnificat in secula"""
```

#### 🔹 Управляющие последовательности (escape-последовательности):

| Последовательность | Что делает            |
|--------------------|------------------------|
| `\\`               | один обратный слеш     |
| `\'`, `\"`         | кавычки внутри строки  |
| `\n`               | новая строка           |
| `\t`               | табуляция (~4 пробела) |

```python
print("Line 1\nLine 2\t→ tab here")
# → Line 1
#   Line 2    → tab here
```

#### 🔹 «Сырые» строки (`r""`)  
Отключают обработку escape-последовательностей — удобно для путей и регулярных выражений.

```python
path = r"C:\python\name.txt"
print(path)  # → C:\python\name.txt  (без ошибки!)
```

#### 🔹 f-строки — форматирование (с Python 3.6+)  
Вставляют значения переменных прямо в строку через `{}`.

```python
user_name = "Tom"
user_age = 37
info = f"name: {user_name}, age: {user_age}"
print(info)  # → name: Tom, age: 37
```

> ✅ Можно даже вычислять внутри: `f"в 2 раза больше: {user_age * 2}"`

---

# 🔍 Как узнать тип переменной?

Функция `type()` возвращает текущий тип значения:

```python
x = 42
print(type(x))     # → <class 'int'>

y = "42"
print(type(y))     # → <class 'str'>

z = True
print(type(z))     # → <class 'bool'>
```

💡 Имя переменной — это **ярлык (ссылка)** на объект в памяти. Сам объект имеет тип, а переменная — нет.

