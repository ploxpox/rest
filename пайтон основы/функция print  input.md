# 🖨️ `print()` — вывод на экран

Функция `print()` — твой главный инструмент для отладки и общения с пользователем.

### 🔹 Основное поведение
- Выводит данные в консоль  
- По умолчанию **добавляет перевод строки `\n`** в конце → следующий `print()` начнёт с новой строки  
- Можно менять это поведение через параметр `end`

### 💡 Примеры

```python
# Простой вывод
print("Привет!")          # → Привет! (и переход на новую строку)

# Несколько значений — через запятую (автоматически добавляется пробел)
print("Hello,", "world!")  # → Hello, world!

# Без перевода строки — продолжаем на той же строке
print("Привет,", end=" ")   # ← end=" " вместо end="\n"
print("как дела?")          # → Привет, как дела?
```

> 📌 `end` может быть любым:  
> `end=""` — вообще ничего не добавлять  
> `end=" → "` — добавить стрелку  
> `end="..."` — точки и т.д.

---

# ⌨️ `input()` — ввод от пользователя

Функция `input()` **останавливает программу** и ждёт, пока пользователь введёт текст и нажмёт **Enter**.

### 🔹 Главное, что нужно знать
- Всегда возвращает **строку** (`str`), даже если ввели число!  
- Текст в скобках — **приглашение** (подсказка для пользователя)

### 💡 Примеры

```python
# Простой ввод с подсказкой
name = input("Введите имя: ")  
# → пользователь вводит, например, "Анна", нажимает Enter
# → name = "Анна" (тип — str!)

print("Привет,", name)  # → Привет, Анна

# Если нужно число — приводим явно!
age_input = input("Сколько вам лет? ")  # ← всё равно строка!
age = int(age_input)  # ← преобразуем в целое число

# Или короче:
height = float(input("Ваш рост в метрах? "))  # ← сразу в float
```

> ⚠️ **Осторожно!**  
> Если пользователь введёт `"двадцать"` вместо `20`, `int()` и `float()` вызовут ошибку.  
> Позже научимся обрабатывать такие случаи — пока просто вводите числа цифрами 😊

---

### 🧪 Мини-пример: диалог

```python
print("Добро пожаловать!")
name = input("Как вас зовут? ")
age = int(input("Сколько вам лет? "))

print(f"Рады знакомству, {name}!")
print(f"Через 10 лет вам будет {age + 10} лет.")
```

**Результат работы:**
```
Добро пожаловать!
Как вас зовут? Алексей
Сколько вам лет? 25
Рады знакомству, Алексей!
Через 10 лет вам будет 35 лет.
```
